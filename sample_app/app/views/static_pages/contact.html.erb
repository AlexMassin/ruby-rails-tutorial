<% provide(:title, "Contact Us") %> 
<h1><%= full_title(yield(:title)) %></h1>
<table>
    <thead>
        <th>Employees</th>
    </thead>
    <tbody>
    <% @names.each do |name| %>
      <tr>
        <td><%= name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
